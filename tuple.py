import string 
l=string.ascii_letters
print(l)

x=list(l)
# x is a list of letter
print(x)

t=tuple(x)
print(t)
# t is the touple we cant change touple 
# we cannot append something to touple and can not remove something to the touple
# touple is the fixed quantity 
# touple use () these barcet to store
# set use {} these bracet to store
# list use [] these braceet to store 
